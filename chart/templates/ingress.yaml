apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules: 
    http:
      paths:
      - path: /nginx
        pathType: Prefix
        backend: 
          service:
            name: service
            port:
              number: 5000