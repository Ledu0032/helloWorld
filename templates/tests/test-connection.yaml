apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zdravoSvete.fullname" . }}-test-connection"
  labels:
    {{- include "zdravoSvete.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zdravoSvete.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
